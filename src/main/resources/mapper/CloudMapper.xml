<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.groupware.project.domain.cloud.mapper.CloudMapper">

    <!-- 하위 요소 조회 -->
    <select id="getElementsOfFolder" resultType="com.groupware.project.domain.cloud.dto.CloudElementDTO">
        SELECT *
        FROM tables.tbl_cloud
        WHERE is_deleted = false
          AND parent_element_key = #{elementKey}::uuid
        ORDER BY (CASE WHEN file_size IS NULL THEN 0 ELSE 1 END), name
    </select>

    <!-- 요소 세부 정보 조회 -->
    <select id="getElementInfo" resultType="com.groupware.project.domain.cloud.dto.CloudElementDTO">
        SELECT *
        FROM tables.tbl_cloud
        WHERE is_deleted = false
        AND element_key = #{elementKey}::uuid
    </select>

</mapper>